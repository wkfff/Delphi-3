unit UICNC_REMK;

interface

uses
  Windows, Messages, SysUtils, Variants, Classes, Graphics, Controls, Forms,
  Dialogs, UBBlankFrm, StdCtrls;

type
  TFICNC_REMK = class(TFBBlankFrm)
    edtCNC_REMK: TEdit;
    Label1: TLabel;
    fbtnOK: TButton;
    fbtnCancel: TButton;
    procedure FormDestroy(Sender: TObject);
  private
    { Private declarations }
  public
    { Public declarations }
  end;

var
  FICNC_REMK: TFICNC_REMK;

implementation

uses ULib;

{$R *.dfm}

procedure TFICNC_REMK.FormDestroy(Sender: TObject);
begin
  inherited;
  FICNC_REMK := nil;
end;

end.
